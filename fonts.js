import { Open_Sans, Hind_Guntur } from '@next/font/google';

export const hindGuntur = Hind_Guntur({
    subsets: ['latin'],
    display: 'swap',
});

export const openSans = Open_Sans({
    subsets: ['latin'],
    display: 'swap',
});
